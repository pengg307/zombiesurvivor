# 🎮 Boss系统说明

## ✅ 触发条件明确

### 射击速度升级触发条件
```
触发时机：玩家升级时
升级选项：射击速度+1x
效果：
• 射速从 1x → 2x → 3x → 4x → 5x
• 子弹发射间隔：1s → 0.5s → 0.33s → 0.25s → 0.2s
```

### 僵尸速度升级触发条件
```
触发时机：玩家选择"射击速度+1x"时
自动触发：僵尸速度+0.5x
上限：3x速度
效果：
• 僵尸移动速度：50px/s → 75px/s → 100px/s → ...
```

### Boss生成触发条件
```
触发时机：击杀数达到100个
前置条件：
• 当前击杀数 >= 100
• Boss尚未生成
• Boss尚未被击败

生成后：
• Boss出现在随机位置
• Boss有500生命值
• Boss有20点伤害
• Boss会发射子弹攻击玩家
```

---

## 🎯 Boss特性

### Boss属性
```gdscript
health: 500.0        # 生命值（普通僵尸30）
damage: 20.0         # 伤害（普通僵尸10）
speed: 50.0          # 速度（与普通僵尸相同）
experience_reward: 100  # 击杀经验（普通僵尸10）
attack_range: 150.0  # 攻击范围（像素）
attack_cooldown: 2.0 # 攻击间隔（秒）
```

### Boss攻击机制
```
1. Boss向玩家移动
2. 当距离 <= 150像素时，Boss可以攻击
3. 每2秒发射一颗子弹
4. 子弹朝向玩家位置
5. 子弹伤害：20点
6. 玩家被击中会受伤并闪烁红色
```

### Boss显示
```
• 颜色：红色（与其他僵尸区分）
• 大小：3倍（比普通僵尸大）
• 标签：显示"⚠️ BOSS ACTIVE!"
```

---

## 📊 游戏流程

### 第一阶段：普通僵尸
```
1. 开始游戏
2. 击杀普通僵尸（basic/fast/tank）
3. 升级获得经验
4. 选择升级选项
5. 积累击杀数
```

### 第二阶段：Boss战
```
1. 击杀达到100个
2. 屏幕显示"⚠️ BOSS INCOMING!"
3. Boss生成在随机位置
4. Boss向玩家移动
5. 玩家必须靠近Boss才能攻击
6. Boss在150像素内可以反击
7. 击败Boss获得大量经验
```

### 第三阶段：胜利
```
1. 击败Boss
2. 屏幕显示"✅ BOSS DEFEATED!"
3. 击杀数达到50（通关条件）
4. 显示胜利面板
5. 可选择重新开始
```

---

## 🔧 可调参数

### 难度调整
```gdscript
// 在EnemySpawner.gd中：
var boss_kills_required: int = 100    // Boss生成所需击杀数
var kills_to_win: int = 50            // 通关所需击杀数
var max_enemies: int = 20             // 同时最大敌人数量

// 在Zombie.gd中：
var boss_attack_range: float = 150.0  // Boss攻击范围
var boss_attack_cooldown: float = 2.0 // Boss攻击间隔
```

### Boss属性调整
```gdscript
// 在Zombie.gd的spawn_boss()中：
boss.health = 500.0       // 调整Boss生命值
boss.damage = 20.0        // 调整Boss伤害
boss.experience_reward = 100  // 调整击杀经验
```

---

## 🎮 操作说明

### PC端
```
WASD/方向键：移动
自动攻击：最近敌人或Boss
升级时：选择升级选项
```

### 移动端
```
触摸屏幕：移动方向
自动攻击：最近敌人或Boss
升级时：点击选项按钮
```

---

## ⚠️ 注意事项

### Boss战策略
```
1. 保持移动，避免被Boss子弹击中
2. 靠近Boss（150像素内）才能安全攻击
3. 利用障碍物（如果有）
4. 优先升级射击速度，快速击败Boss
5. 升级生命值，提高生存能力
```

### 击杀计数
```
• 击杀普通僵尸：+1
• 击杀Boss：+100（一次性）
• 通关条件：击杀50个普通僵尸 + 击败Boss
```

---

**Boss系统已完成！现在用Godot编辑器测试吧！** 🎮
