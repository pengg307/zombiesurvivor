# 坐标系统修复完成

**日期**: 2026-08-07
**状态**: ✅ 已验证

---

## 问题诊断

**根本原因**: 坐标系不一致导致僵尸生成在屏幕外

| 实体 | 原坐标系统 | 问题 |
|------|-----------|------|
| Player | 屏幕坐标 (360, 1100) | 正常 |
| Bullet | 屏幕坐标 | 正常 |
| Zombie | 混合使用 | 位置错误 |
| 生成器 | 屏幕坐标 | 位置错误 |

---

## 修复方案

### 统一坐标系
```
中心坐标系（僵尸/油桶）:
  原点 = 屏幕中心 (360, 640)
  X轴: 左负右正
  Y轴: 上负下正

屏幕坐标系（玩家/子弹/UI）:
  原点 = 左上角 (0, 0)
```

### 转换公式
```gdscript
# 中心坐标 → 屏幕坐标
screen_x = zombie_x + 360
screen_y = zombie_y + 640

# 屏幕坐标 → 中心坐标  
zombie_x = screen_x - 360
zombie_y = screen_y - 640
```

### 生成位置（修正后）
| 位置 | 中心坐标 | 屏幕坐标 | 可见性 |
|------|---------|---------|--------|
| 玩家 | (0, 460) | (360, 1100) | 底部中央 |
| 左侧僵尸 | (-100, -200) | (260, 440) | ✅ 左上可见 |
| 右侧僵尸 | (100, -200) | (460, 440) | ✅ 右上可见 |

---

## 验证结果

```bash
$ Godot --headless --scene "res://scenes/Game.tscn"

✅ Player创建成功 - 位置:(360.0, 1100.0)
✅ Zombie已创建 - 类型:tank血量:20.0
   中心坐标:(-100.0, -200.0)
   屏幕坐标:(260.0, 440.0)  ← 可见位置
✅ Zombie spawned at: (-100, -200) - 屏幕位置: (260, 440) (左侧)
✅ 无脚本错误
```

---

## 交替生成逻辑

```gdscript
var spawn_side = 0  # 0=左侧, 1=右侧

func _spawn_square():
    if spawn_side == 0:
        start_x = -100.0  # 左侧
    else:
        start_x = 100.0   # 右侧
    
    spawn_side = 1 - spawn_side  # 交替切换
```

---

## 修改的文件

| 文件 | 关键修改 |
|------|---------|
| EnemySpawner.gd | 使用中心坐标，修复交替逻辑 |
| Zombie.gd | 添加坐标转换方法，修正移动逻辑 |
| Player.gd | 信号连接修复 |
| GameManager.gd | 延迟信号连接避免时序问题 |

---

## 下一步

Phase 3 可以开始：
- 粒子特效
- 屏幕震动
- 角色素材替换
