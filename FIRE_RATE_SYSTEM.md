# 🎮 射击速度系统 - 修正版

## ✅ 新的射击速度逻辑

### 基准速度
```
初始状态：2x 射速（0.5秒/发）
普通状态：2x + 升级加成
危险状态：1x（生命值 <= 20%时）
```

### 速度变化规则
```
正常情况（生命值 > 20%）：
• 初始：2x 射速
• 升级：每次+1x（最多到5x）
• 公式：总射速 = 2x + 升级加成

危险情况（生命值 <= 20%）：
• 强制：1x 射速
• 恢复：生命值回到20%以上时恢复
```

---

## 📊 速度对照表

| 状态 | 生命值 | 射速 | 射击间隔 |
|------|--------|------|----------|
| 初始 | 100% | 2x | 0.5秒 |
| +1级 | 100% | 3x | 0.33秒 |
| +2级 | 100% | 4x | 0.25秒 |
| +3级 | 100% | 5x | 0.2秒 |
| 危险 | ≤20% | 1x | 1.0秒 |

---

## 🎯 触发条件

### 危险状态触发
```
触发条件：current_health <= max_health * 0.2
效果：射击速度降为1x
提示：显示"⚠️ 危险状态！射击速度降至1x"
```

### 恢复状态触发
```
触发条件：current_health > max_health * 0.2
效果：射击速度恢复为基准速度
提示：显示"✅ 状态恢复！射击速度恢复为Nx"
```

### 升级触发
```
触发条件：玩家升级时选择"射击速度+1x"
效果：基准速度+1x（危险状态除外）
上限：5x射速
```

---

## 🔧 代码实现

### Player.gd关键逻辑
```gdscript
# 基准参数
var base_attack_cooldown: float = 1.0  # 基础间隔（1秒）
var attack_cooldown: float = 0.5       # 当前间隔（2倍速）
var fire_rate_bonus: float = 0.0       # 升级加成

# 检查危险状态
func _check_critical_health() -> void:
    var health_ratio = current_health / max_health
    if health_ratio <= 0.2:
        # 危险：降为1x
        attack_cooldown = base_attack_cooldown
    else:
        # 恢复：使用基准速度
        _update_fire_rate()

# 升级射击速度
func upgrade_fire_rate() -> void:
    if health_ratio > 0.2:  # 只在安全状态升级
        fire_rate_bonus += 1.0
        _update_fire_rate()
```

---

## 💡 游戏平衡

### 设计思路
```
1. 初始2x射速：保证前期游戏体验流畅
2. 危险时降为1x：增加紧张感，符合"濒危"设定
3. 可升级到5x：后期爽快感
4. 恢复机制：鼓励玩家治疗/躲避
```

### 战斗策略
```
• 保持生命值在20%以上才能维持高射速
• 危险时优先躲避，不要硬拼
• 升级射击速度提升输出
• 合理分配升级点（射速/伤害/生命）
```

---

## 📝 测试要点

### 速度验证
```
□ 初始射速为2x（0.5秒/发）
□ 升级后射速提升
□ 生命值<20%时降为1x
□ 生命值>20%时恢复正常
□ 危险状态无法继续升级射速
```

### 视觉反馈
```
□ 危险状态有文字提示
□ 恢复状态有文字提示
□ UI显示当前射速
□ 升级时显示射速变化
```

---

**射击速度系统已修正！现在用Godot编辑器测试吧！** 🎮
